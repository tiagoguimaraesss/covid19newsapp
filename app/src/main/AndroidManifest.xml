<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.covid19newsapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.activity.SplashScreenActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppCompat.FullScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activity.OnBoardingOneActivity"
            android:theme="@style/AppCompat.FullScreen" />
        <activity
            android:name=".ui.activity.OnBoardingTwoActivity"
            android:theme="@style/AppCompat.FullScreen" />
        <activity
            android:name=".ui.activity.OnBoardingThreeActivity"
            android:theme="@style/AppCompat.FullScreen" />
        <activity
            android:name=".ui.activity.LoginActivity"
            android:theme="@style/AppCompat.FullScreen" />
        <activity
            android:name=".ui.activity.HomeActivity"
            android:theme="@style/AppCompat.FullScreen" />
        <activity
            android:name=".ui.activity.ResumoNoticiaActivity"
            android:theme="@style/AppCompat.FullScreen" />
    </application>
</manifest>